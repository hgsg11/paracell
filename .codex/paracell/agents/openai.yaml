interface:
  display_name: "Paracell Requirement Dispatcher"
  short_description: "Dispatch explicit parallel work to Paracell"
  default_prompt: "Use $paracell to clarify this explicitly parallel request, then dispatch it to the best matching Paracell template."
