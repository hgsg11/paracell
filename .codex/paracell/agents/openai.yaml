interface:
  display_name: "Paracell Code Change Dispatcher"
  short_description: "Dispatch configured development changes"
  default_prompt: "Use $paracell for this system-changing development task when the target project contains paracell.yaml, then create an issue-backed work package and dispatch it to the best matching template."
