interface:
  display_name: "Paracell Code Change Dispatcher"
  short_description: "Dispatch or retry configured development changes"
  default_prompt: "Use $paracell to create an issue-backed work package, then dispatch it with the best template or retry its existing failed cell."
